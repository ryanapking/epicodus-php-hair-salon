<!DOCTYPE html>
<html>
    <head>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <title>Stylists</title>
    </head>
    <body>
        <div class='container'>
            <h1>Awesome Hair Salon</h1>
            <h4>Current stylists:</h4>
            <ul>
                {% for stylist in stylists %}
                    <li>{{ stylist.getName }}</li>
                {% endfor %}
            </ul>
            <hr>
            <h4>Add new stylist:</h4>
            <form class='form form-inline' action='/stylists' method='post'>
                <input class='form-control' type='text' name='stylist_name' placeholder="stlyist's name">
                <button class='form-control btn btn-info' type='submit'>Add Stylist</button>
            </form>
        </div>
    </body>
</html>
